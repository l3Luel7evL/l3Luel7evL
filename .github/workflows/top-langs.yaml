name: Show Top Languages

on:
  schedule:
    - cron: '0 * * * *'  # This will run the action every hour
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Generate Language Card
        uses: anuraghazra/github-readme-stats@v2.1.0  # Use the latest version available
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          username: 'l3Luel7evL'  # Replace with your GitHub username
          theme: 'radical'  # You can choose any theme you like
          hide_title: false
          hide_border: true
          show_icons: true
          show_owner: true
          count_private: true  # Include private repo languages
